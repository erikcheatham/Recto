<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:networkSecurityConfig="@xml/network_security_config">
        <!-- FCM auto-registers a default FirebaseMessagingService at install
             time when google-services.json is present. We don't subclass it;
             the AndroidFcmPushTokenService just calls FirebaseMessaging.GetToken()
             on demand. Receiving push wakeups happens via the framework's
             default service forwarding to the OnNewToken / OnMessageReceived
             overrides if we add them later (round 8.x optional). -->
        <!--
          NOTE on URL-scheme intent-filter (Task #22 Phase 1, banked
          2026-05-31): the recto:// deep-link intent-filter is declared
          via [IntentFilter] attributes on MainActivity in
          MainActivity.cs, NOT here. MAUI's Android build emits the
          MainActivity declaration into the merged AndroidManifest.xml
          from the [Activity] attribute + any sibling [IntentFilter]
          attributes; manually re-declaring the activity here would
          conflict at manifest-merge time. See MainActivity.cs for the
          actual [IntentFilter] declaration.
        -->
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Android 13+ (API 33) requires explicit runtime permission for
         notifications. Earlier API levels grant by default. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Build 6 (2026-06-02): CAMERA permission + camera hardware
         feature retired with the in-app QR scanner. OS Camera handles
         the QR-scan UX off-app; the recto:// URL-scheme deep-link
         picks the payload back up via MainActivity.OnNewIntent. No
         in-app camera surface remains, so the permission declaration
         is no longer required. -->
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />
    <uses-feature android:name="android.hardware.strongbox_keystore" android:required="false" />
</manifest>